# v2 Phase 1 Follow-ups (durable tracker)

Registered by the 2026-07-23 retro (PR #3). Status updates belong here, not in PR comments.

| # | Item | Type | Priority | Status |
|---|---|---|---|---|
| F1 | Run spec SC1-SC4 validation. **SC1 MET** (2026-07-23: 63/65 digestible = 96.9% success; the 2 failures were Fable-5 AUP refusals, see F6, not code). **SC2 MET** (2026-07-23: launchd job installed at ~/Library/LaunchAgents, kickstart digested a dropped PDF with `run_origin=scheduled`, zero manual command; err log clean after adding node dir to plist PATH). **SC3 MET** (2026-07-29: 5/5 re-find queries answered from the wiki — 비트코인, 유심 해킹, 성전환, 태영건설, 이란 상공; note: FTS5 multi-word compound queries can miss, single keywords reliable). **SC4 MET** (2026-07-29: 7/23–7/29 validation week — 26 scheduled, 2 interactive; the 2 interactive entries were F9 max_file_size testing on 7/27, not regular use). **Done** — all four success criteria met | feature/process | P1 | Done |
| F2 | Deferred review minors batch: 26 items addressed across config, storage, index, llm, ingest, CLI, MCP, integration tests. U6-m6 already fixed by orphan sweep PR #14. All tests pass. **Done** (PR #18, 2026-08-08) | process | P3 | Done |
| F3 | FTS write-write `SQLITE_BUSY_SNAPSHOT` limitation — accepted limitation. Deviation documented at `docs/deviations/2026-07-22-busy-timeout-fts-write-write.md`; SPEC/DESIGN corrected; flock prevents ingest×ingest; residual ingest-vs-serve window is classInfra (self-heals). No real-world collisions observed through 2026-08-08. **Done** (closed as documented/accepted, 2026-08-08) | architecture | P3 | Done |
| F4 | Spec self-contradiction: resolved via orphan sweep + archive (Option C). Pages for missing sources archived to `sources/_archived/`, ledger rows superseded. SPEC §10.2 documents rename behavior. Design spec's "including renamed files" removed (never implemented). **Done** (2026-08-03, PR #14, main 45ce6bb) | edge-case | P3 | Done |
| F5 | Cleanup: ~~dead `contentHash()` in internal/ingest~~ (stale — actively used by ledger/orphan-sweep), v1-shaped `.cogvault.yaml` fixtures under testdata/fixtures/ (removed), MCP missing-schema fallback tells agents to `wiki_read("_schema.md")` which 404s (fixed: `defaultSchemaInstructions` now embeds `schema.DefaultContent`). **Done** (PR #17, 2026-08-08) | process | P4 | Done |
| F6 | Real backlog run surfaced Fable-5 AUP safety-filter refusals classified as transient `exit status 1` — retries forever with no progress (2/65 files, e.g. eLife paper, mRNA article). **Done** (PR #8, main f8a5b13): new terminal `refused` error class (detects api_error terminal OR "API Error:"/"safeguards flagged" body), `llm.model` config knob → `claude --model`, per-row model recorded so switching to opus/sonnet auto-recovers refused files on the next scheduled run. Retro: docs/retros/2026-07-23-aup-error-class-and-model-option-retro.md. Follow-up: user may set `llm.model: opus` in config to route future biomedical/policy content to a non-refusing model | feature/edge-case | P2 | Done |
| F7 | Slug quality for non-ASCII filenames: `slugFor` ASCII-only `[a-z0-9._-]` filter replaced with `unicode.IsLetter`/`unicode.IsDigit`. Korean/CJK filenames now produce readable slugs (`판례_95도250.pdf` → `판례_95도250.md`). **Done** (2026-07-29, PR #13, main e614799): 14 table-driven test cases pass, full suite regression-free. SPEC §10.2 updated. No migration — existing `src-<hash8>` pages persist until source content changes | developer-experience | P3 | Done |
| F8 | Source-type differentiation via `category` frontmatter field. LLM classifies ingested pages as `article` \| `legal` \| `reference` via `buildPrompt` instruction. Category stored in frontmatter and indexed in `file_meta` (schemaVersion 3). `NormalizeCategory` coerces off-taxonomy values. **Done** (2026-07-29, main): SC1 validated — `판례_95도250.pdf` ingested with `category: legal`, CLI shows `(source/legal)`, MCP results include category. SC2 backward compat — existing 91 pages functional with `category: ""`. Spec: `docs/specs/2026-07-29-source-category-classification-design.md` | architecture/feature | P2 | Done |
| F9 | Configurable max file size: the 32MB `maxFileSize` cap was hardcoded. Exposed `max_file_size_mb` in config.yaml (default 32, top-level, negative rejected). **Done** (PR #10, main 3b3659f): config field + wiring + SPEC/DESIGN updated. SC1b validated: 42MB NYT PDF digested with `max_file_size_mb: 64`. Retro: docs/retros/2026-07-27-configurable-max-file-size-retro.md | feature | P2 | Done |
| F10 | Add `codesign --force --sign -` to Makefile build target: rebuilt Go binaries get SIGKILL from macOS because FDA tracks code hash, not path. The codesign step after `go build` prevents this. Surfaced during F9 deployment. **Done** (PR #11, main b12782f): Makefile with build/install/test/clean targets; key finding: install target must re-sign at destination path (sign-then-copy alone is insufficient). Retro: docs/retros/2026-07-27-makefile-codesign-retro.md | process | P3 | Done |
| F11 | Documentation drift from roadmap clearance: `SPEC.md` §1.3 "Out of scope (Phase 1)" still lists vector search, ontology graph, `ResolveLink`, periodic `cogvault digest`, phone capture, URL/web extraction, and the local LLM backend, all of which `ROADMAP.md` records as delivered; and `ROADMAP.md:40` still lists F4 as an open follow-up while this tracker marks it Done. Surfaced during the 2026-08-11 remote-MCP-server work, which corrected only the `wiki_delete`/auto-commit entry — the one its own change would otherwise have contradicted — and deliberately left the rest rather than widening that feature's scope. Spec: `docs/specs/2026-08-11-remote-mcp-server-design.md` (Integration, Deferred to Follow-Up Work) | process | P3 | Open |
